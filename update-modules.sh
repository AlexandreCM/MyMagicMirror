# Go to the modules directory
cd ~/MagicMirror/modules
# Clone the wanted modules and pull the latest changes
git clone <repository_url_1> && git -C <repository_directory_1> pull
git clone <repository_url_2> && git -C <repository_directory_2> pull

# go back home
cd ~